# Expected schema (user-supplied hint)

Before observing anything, the person running this was asked what the source
is expected to roughly contain. They said:

{{EXPECTED_SCHEMA_TEXT}}

Treat this as a hint about where to look, not as fact. It was written before
the schema was ever queried and may be incomplete, outdated, or use different
names than the database does. Verify every table and column against the
actual observations below before trusting anything here.

If the live schema does not match this description, follow the data: note the
mismatch as a gap or an uncertainty rather than assuming a table was renamed,
merged, or is simply missing.
